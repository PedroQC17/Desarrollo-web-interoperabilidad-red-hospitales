server:
  port: 8002
  name: pacientes-service

database:
  engine: django.db.backends.postgresql
  name: db_pacientes
  user: postgres
  password: postgres
  host: postgres-pacientes
  port: 5433

services:
  auth: "http://auth-service:8001"

consul:
  host: consul
  port: 8500
